require 'rspec'
require './lib/student'


describe Student do
  describe '#initialize' do
      it 'is an instance of student' do
        # write the code to initialize a new student object
        student = Student.new(Anne)
        expect(student).to be_a student
      end
      @name = name
      # test it has cookies
      # test it can add cookies
  
  
  
    end
end
